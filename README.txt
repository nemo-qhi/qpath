qpath additional patch: quiz retry flow

Upload these files to the existing GitHub repository and commit changes:
- src/app.js
- src/styles.css

Behavior included:
- After a wrong answer, choosing the next action sends that quiz to the end of the current session.
- The quiz appears again until answered correctly.
- After all current-session quizzes are answered correctly, the user can return home with one tap.
